pipeline:
  name: default_rag_pipeline
  steps:
    - indexing
    - pre_retrieval
    - retrieval
    - post_retrieval
    - generation
    - evaluation

indexing:
  document_processing:
    chunk_size: 1000
    chunk_overlap: 200
